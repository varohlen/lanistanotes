---
import { getCollection } from 'astro:content';

const notes = await getCollection('changenotes');
const latestNote = notes.sort((a, b) => b.data.season - a.data.season)[0];

return Astro.redirect(`/notes/${latestNote.slug}`, 301);
---
